<!--components/pagination/index.wxml-->
<view class="pagination">
  <label wx:if="prevUrl" class="btn prev-btn" bindtap="prev">上一页</label>
  <label class="btn currPageInfo">{{currPageNum + '/' + totalPageNum}}</label>
  <label wx:if="nextUrl" class="btn next-btn" bindtap="next">下一页</label>
</view>